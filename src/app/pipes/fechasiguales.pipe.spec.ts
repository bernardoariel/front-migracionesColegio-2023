import { FechasigualesPipe } from './fechasiguales.pipe';

describe('FechasigualesPipe', () => {
  it('create an instance', () => {
    const pipe = new FechasigualesPipe();
    expect(pipe).toBeTruthy();
  });
});
